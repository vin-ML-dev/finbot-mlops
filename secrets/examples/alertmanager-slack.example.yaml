# EXAMPLE — the Slack webhook Alertmanager uses for Path 1. DO NOT commit real values.
# kube-prometheus-stack mounts this secret at /etc/alertmanager/secrets/...
apiVersion: v1
kind: Secret
metadata:
  name: alertmanager-slack
  namespace: monitoring
type: Opaque
stringData:
  webhook: "https://hooks.slack.com/services/REPLACE/ME/PLEASE"
