# EXAMPLE — the monitoring agent's secrets. DO NOT commit real values.
# OPENAI_API_KEY is optional (only if the agent's llm backend is "openai").
apiVersion: v1
kind: Secret
metadata:
  name: agent-secret
  namespace: finbot
type: Opaque
stringData:
  SLACK_WEBHOOK: "https://hooks.slack.com/services/REPLACE/ME/PLEASE"
  GATEWAY_API_KEY: "REPLACE_WITH_THE_SAME_GATEWAY_KEY"
  OPENAI_API_KEY: "REPLACE_OR_LEAVE_BLANK"
