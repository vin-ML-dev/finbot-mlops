# Service clients (the gateway) connect to: llama-cpp-svc:8080
apiVersion: v1
kind: Service
metadata:
  name: llama-cpp-svc
  namespace: finbot
  labels:
    app: llama-cpp
spec:
  selector:
    app: llama-cpp
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP
