# EXAMPLE — the plaintext Secret the model needs. DO NOT commit real values here.
# You seal this (see secrets/README.md) into secrets/sealed/model-sealed.yaml.
apiVersion: v1
kind: Secret
metadata:
  name: model-secret
  namespace: finbot
type: Opaque
stringData:
  HF_TOKEN: "REPLACE_WITH_YOUR_HUGGINGFACE_TOKEN"
