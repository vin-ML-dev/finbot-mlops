# EXAMPLE — the gateway's front-door API key. DO NOT commit real values.
apiVersion: v1
kind: Secret
metadata:
  name: gateway-secret
  namespace: finbot
type: Opaque
stringData:
  GATEWAY_API_KEY: "REPLACE_WITH_A_STRONG_RANDOM_KEY"
